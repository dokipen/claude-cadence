---
name: lead
description: Coordinate implementation work through structured phases with specialist agents. All work is tracked via a ticket provider (GitHub Issues or issues-api).
---

# Lead

This is a Codex entrypoint wrapper for a Claude Cadence user-facing workflow.

Source of truth:
- `../../../commands/lead/SKILL.md`
- `../../../CLAUDE.md`

Instructions:
1. Read `../../../commands/lead/SKILL.md` before taking action.
2. Read `../../../CLAUDE.md` for repo-wide Cadence rules, ticket provider configuration, and verification commands.
3. Treat Claude slash-command references as this Codex skill.
4. When the source workflow says to delegate with an agent, use Codex sub-agents when available or perform the work locally when that keeps the critical path moving.
5. Do not duplicate or fork the workflow here unless Codex-specific packaging requires it.
6. When this wrapper and the source file disagree, follow `../../../commands/lead/SKILL.md` and update the wrapper generation rather than patching the generated file manually.

Canonical description: Coordinate implementation work through structured phases with specialist agents. All work is tracked via a ticket provider (GitHub Issues or issues-api).

<!-- Generated by scripts/sync-codex.py. Edit the Claude source file or the generator, then re-run the script. -->
